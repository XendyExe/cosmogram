[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cosmogram"
requires-python = ">=3.8"
version = "0.1.0"

[tool.maturin]
features = ["extension-module"]
module-name = "cosmogram"